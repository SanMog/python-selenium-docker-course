# Проект: Автоматизация на Python с Selenium, Docker и CI/CD

Это учебный проект, демонстрирующий полный цикл создания UI-автотеста: от написания кода на Python до его автоматического запуска в Docker-контейнере с помощью CI/CD (GitHub Actions).

## 🚀 Технологии в проекте

*   **Python:** Язык программирования для написания тестов.
*   **Pytest:** Фреймворк для запуска тестов и использования фикстур.
*   **Selenium:** Библиотека для автоматизации действий в веб-браузере.
*   **Page Object Model (POM):** Паттерн проектирования для структурирования кода.
*   **Docker:** Система контейнеризации для создания переносимого и стабильного окружения.
*   **GitHub Actions:** Встроенная система CI/CD для автоматического запуска тестов.

## ✨ Что делает этот проект?

1.  Запускает браузер Chrome.
2.  Открывает главную страницу Википедии.
3.  Вводит в поле поиска "Python" и нажимает "Найти".
4.  Проверяет, что заголовок страницы с результатами содержит слово "Python".
5.  Весь процесс инкапсулирован в Docker-контейнер и запускается автоматически при каждом изменении кода.

## ⚙️ Как запустить локально

Для запуска проекта на вашем компьютере должны быть установлены **Git** и **Docker Desktop**.

1.  **Клонируйте репозиторий:**
    ```bash
    git clone https://github.com/SanMog/python-selenium-docker-course.git
    ```

2.  **Перейдите в папку проекта:**
    ```bash
    cd python-selenium-docker-course
    ```

3.  **Соберите Docker-образ:**
    Эта команда прочитает `Dockerfile` и создаст готовый образ со всеми зависимостями.
    ```bash
    docker build -t wiki-test .
    ```

4.  **Запустите тесты:**
    Эта команда запустит контейнер из созданного образа, выполнит `pytest` и автоматически удалит контейнер после завершения.
    ```bash
    docker run --rm wiki-test
    ```

## 📂 Структура проекта

*   `Dockerfile`: Инструкция по сборке Docker-образа.
*   `requirements.txt`: Список Python-библиотек для установки.
*   `wikipedia_page.py`: Page Object, описывающий страницу Википедии и методы для взаимодействия с ней.
*   `test_wikipedia_search.py`: Файл с тестом, который использует `WikipediaPage` для выполнения шагов.
*   `.github/workflows/main.yml`: Файл с инструкцией для CI/CD пайплайна на GitHub Actions.

## 🤖 CI/CD Пайплайн

Проект настроен на автоматический запуск тестов с помощью GitHub Actions. Процесс запускается при каждом `push` в ветки `main` или `master`.

Результаты выполнения можно посмотреть во вкладке **Actions** вашего репозитория на GitHub.